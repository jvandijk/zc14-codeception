class_name: MigrateTester
modules:
    enabled:
        - MigrateHelper
        - Filesystem
        - Cli
env:
    remote:
        modules:
            enabled:
                - MigrateHelper
                - Filesystem
                - Cli
                - FTP
            config:
                FTP:
                    type: sftp
                    host: '127.0.0.1'
                    port: 22
